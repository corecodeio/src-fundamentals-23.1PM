function validParentheses(parens) {
  // 1. recorrer el string
  // 2. identificar si el parentesis es correcto
  ///  2.1. Antes de un ) debe haber un (
  // 3. ( <-- si despues de ( no hay uno que lo cierre al final

  // Input: ' .... '
  // Output: boolean

  return false;
}

// https://developer.mozilla.org/es/docs/Web/JavaScript/Reference/Global_Objects/Array/Reduce
